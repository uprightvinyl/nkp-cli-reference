# `nkp import`

```text
Import images from an image bundle into Containerd

Usage:
  nkp import [command]

Available Commands:
  image-bundle Import images from image bundles into Containerd

Flags:
  -h, --help          Help for import
  -v, --verbose int   Output verbosity

Use "nkp import [command] --help" for more information about a command.
```
