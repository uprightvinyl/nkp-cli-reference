# `nkp install`

```text
Install one of [kommander]

Usage:
  nkp install [command]

Available Commands:
  kommander   Install kommander

Flags:
      --config string            Config file to use (default "/home/runner/.kommander/config")
      --context string           The name of the kubeconfig context to use
  -h, --help                     Help for install
      --kubeconfig string        Path to the kubeconfig file to use for CLI requests.
      --request-timeout string   The length of time to wait before giving up on a single server request. Non-zero values should contain a corresponding time unit (e.g. 1s, 2m, 3h). A value of zero means don't timeout requests.
  -v, --verbose int              Output verbosity

Use "nkp install [command] --help" for more information about a command.
```

## Subcommands

- [`nkp install kommander`](kommander/nkp-install-kommander.md)
